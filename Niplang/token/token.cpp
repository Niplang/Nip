#include <string>
#include <vector>

std::vector<std::string> token (std::string str) {
    // The vector that returns the lexeme of the given token
    std::vector<std::string> lexeme;

    return lexeme;
}